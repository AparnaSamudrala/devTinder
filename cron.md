cron ->

# https://www.npmjs.com/package/node-cron

The node-cron module is tiny task scheduler in pure JavaScript for node.js based on GNU crontab. This module allows you to schedule task in node.js using full crontab syntax.
npm i node-cron

# https://crontab.guru/

# to test the cron string for scheduled exact time

date-fns provides the most comprehensive, yet simple and consistent toolset for manipulating JavaScript dates in a browser & Node.js

# ==> https://www.npmjs.com/package/date-fns

# npm i date-fns

when we have to process lakhs of emails we cannot loop in a for of like we did in cronjob.js we have to use

# npm bee-queue & npm bull packages to queue them up asyncly

or Amazon SES will handle bulk requests to handle this read about amazon SES bulk emails

# make sendEmail function dynamic
